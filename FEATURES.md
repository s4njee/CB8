# CB8 — Features

A snapshot of what ships today and what's on the backlog. Generated by walking the codebase, not the README; if something isn't listed under **Implemented** it isn't in the build. Each implemented feature carries a sublist of the files that own it so the docs and the source stay legible together.

> **PLAN10 in progress:** `src/web/` is the canonical frontend; `src/renderer/`
> entries below are being collapsed onto the same SPA and should not receive
> new product work. See `PLAN10.md`.

---

## Implemented

### Library / Catalog

- **SQLite library DB with versioned migrations**, WAL mode, `busy_timeout`, idempotent post-migration indexes.
  - `src/main/db/schema/create.ts` — base DDL for all tables (comics, users, sessions, accounts, libraries, folders, tags, app_meta, …).
  - `src/main/db/schema/migrations.ts` — `migrateSchema`, version detection, `ensurePostMigrationIndexes`, `ensureSearchIndex`.
  - `src/main/db/schema/open.ts` — `openOrRecreate` (PRAGMAs, recovery).
  - `src/main/db/schema/repairs.ts` — one-shot data repairs (`backfillCompletedOnFinalPage`, etc.).
  - `src/main/libraryDatabase.ts` — facade that fans calls out to per-domain modules.
  - `src/main/db/{comics,libraries,folders,tags,users,progress,bookmarks,history,favorites,appMeta}.ts` — domain queries.
- **FTS5 full-text search** over title / file_path / series / author / summary, with prefix-matching and Unicode-aware tokenization (≤7 ms typical at 100 k rows).
  - `src/main/db/schema/migrations.ts` — `ensureSearchIndex` (virtual table + insert/update/delete triggers + lazy backfill).
  - `src/main/db/comics.ts` — `buildFtsQuery`, plus the search branch in `queryComics` and `queryComicsForUser`.
  - `scripts/bench-search.mjs` — synthetic 100 k-row benchmark, LIKE vs FTS5.
- **Auto-recovery on corrupt DB**: sidecars unlinked, schema recreated.
  - `src/main/db/schema/open.ts` — `openOrRecreate`, `DbStartupError`.
  - `src/main/index.ts` — startup error handling around `db.initialize()`.
- **Comic + book ingestion** for `.cbz`, `.cbr`, `.epub`, `.pdf`, `.mobi`.
  - `src/shared/mediaTypes.ts` — extension allowlist + media-type classification.
  - `src/main/ingestService.ts` — `IngestService.addFile` / `scanDirectory`.
  - `src/main/fileScanner.ts` — `FileScannerImpl` (walks dirs, defers to ingest).
  - `src/main/archiveLoader.ts` — CBZ via `yauzl`, CBR via `node-unrar-js`, JXL transcode.
  - `src/main/epubCoverExtractor.ts`, `src/main/pdfCoverExtractor.ts` — book cover pipelines.
- **Async file scanner with cancellable AbortController**, progress events, dismissed-path tracking.
  - `src/main/ingestService.ts` — `scanDirectory(signal)`, recursive `discoverFiles(signal)`.
  - `src/main/fileScanner.ts` — `FileScannerImpl.scan` / `.scanBooks` (signal-threaded).
  - `src/main/ipc/libraryHandlers.ts` — `library:scan`, `library:scan-books`, `library:scan-cancel` (single shared `AbortController`).
  - `src/renderer/components/library/hooks/useScanProgress.ts` — renderer-side progress state.
  - `src/main/db/comics.ts` — `dismissed_paths` writes on remove + check on rescan.
- **Drag-and-drop import** (single files, multiple files, folders, mixed).
  - `src/shared/dropValidator.ts` + `src/shared/dropValidator.test.ts` — extension allowlist and validation.
  - `src/renderer/components/LibraryView.tsx` — `handleDragOver/Leave/Drop`.
  - `src/main/ipc/libraryHandlers.ts` — `library:classify-paths`, `library:add-files`.
  - `src/web/admin/drop.js` — DataTransfer traversal for the web UI upload flow.
- **Cover thumbnail extraction** for CBZ, CBR, EPUB, PDF.
  - `src/shared/coverSelection.ts` — picks "best" image from an archive.
  - `src/main/archiveLoader.ts` — `getCoverImage`.
  - `src/main/epubCoverExtractor.ts` — EPUB cover XHTML / OPF cover.
  - `src/main/pdfCoverExtractor.ts` — first-page render via `pdfjs-dist`.
  - `src/main/thumbnailGenerator.ts` — Sharp resize + JPEG.
- **Series auto-detection from filename** (`Series Vol. N` style).
  - `src/main/seriesParser.ts` + `src/main/seriesParser.test.ts`.
  - `src/main/db/comics.ts` — `setComicSeries`, `getAllSeries`, `getSeriesComics`.
  - `src/main/webServer/routes/comics.ts` — `/api/series` and `/api/series/:name/comics` endpoints.
- **Per-comic tags** (add / remove / list / rename / delete / bulk).
  - `src/main/db/tags.ts` — primary store + queries.
  - `src/main/webServer/routes/tags.ts` — REST surface (`PUT /api/tags/:name`, `DELETE /api/tags/:name`, `PUT /api/comics/:id/tags`).
  - `src/main/ipc/libraryHandlers.ts` — `library:add-tag`, `library:remove-tag`, `library:add-tag-bulk`, `library:remove-tag-bulk`, `library:rename-tag`, `library:delete-tag`, `library:get-tags`.
- **Virtual folders** (cross-library grouping) and **libraries** (typed comic vs book).
  - `src/main/db/libraries.ts`, `src/main/db/folders.ts`.
  - `src/main/webServer/routes/libraries.ts`, `src/main/webServer/routes/folders.ts`.
  - `src/main/ipc/libraryHandlers.ts` — `libraries:*` and `folders:*` channels.
  - `src/renderer/components/LibrarySidebar.tsx` — renderer UI + create/rename/delete.
  - `src/web/app/sidebar.js`, `src/web/app/sideContextMenu.js` — web-UI sidebar.
- **Filters**: media type, file extension, read status, tag, favorites, free-text search.
  - `src/shared/types.ts` — `QueryOptions`, `FilterPreset`.
  - `src/shared/filterLogic.ts` — `parseFilterPreset`.
  - `src/main/db/comics.ts` — `queryComics`, `queryComicsForUser`.
  - `src/main/webServer/middleware.ts` — `parseQueryOptions` (translates `?…` → `QueryOptions`).
  - `src/renderer/components/library/hooks/useLibraryFilters.ts`, `src/renderer/components/FilterBar.tsx`.
  - `src/web/views/library/strips.js` — mobile filter pills.
- **Sort** by title / dateAdded / fileSize / pageCount / lastRead, asc/desc; covering indexes.
  - `src/main/db/types.ts` — `SORT_COLUMN_MAP`.
  - `src/main/db/schema/migrations.ts` — `idx_comics_media_title`, `idx_comics_media_date_added`, `idx_comics_media_last_read`.
  - `src/renderer/components/SortControl.tsx`, `src/web/app/sort.js` — UI.
- **Pagination + virtualization**.
  - `src/renderer/components/LibraryView.tsx` — `useVirtualizer` (Tanstack) over rows.
  - `src/web/views/library.js` — `IntersectionObserver` infinite scroll, `loadNextPage`.
- **Filter preset persistence** in `app_meta`.
  - `src/main/db/appMeta.ts`, `src/main/ipc/appHandlers.ts`.
  - `src/renderer/components/LibraryView.tsx` — `persistAndReset` writes preset.
- **Folder cards on the All view**; foldered comics excluded from the loose grid.
  - `src/web/views/library.js` — `excludeFoldered: true`, folder fetch on first page.
  - `src/web/views/library/cards.js` — `createFolderCard`.
  - `src/renderer/components/library/FolderCard.tsx`, `FolderContextMenu.tsx`.
- **Multi-select with shift-range, bulk delete, bulk tag**.
  - `src/renderer/components/library/hooks/useLibrarySelection.ts`.
  - `src/web/views/library/selection.js`.
  - `src/web/admin/bulkDelete.js`.
- **"Continue Reading" shelf and "Recently Read" view**.
  - `src/main/db/comics.ts` — `getRecentlyRead`, `getContinueReading`.
  - `src/main/db/progress.ts` — per-user variants.
  - `src/main/webServer/routes/progress.ts` — `/api/recently-read`, `/api/continue-reading`.
  - `src/renderer/components/ContinueReadingShelf.tsx`.
  - `src/web/views/library.js` — `renderContinueShelf`.
- **Reading progress** (per-comic + per-user) and auto-completion on final page.
  - `src/main/db/comics.ts` — `updateReadingProgress`, `updateReadingLocation`.
  - `src/main/db/progress.ts` — `upsertUserProgress`, `clearUserProgress`, `getUserProgress`.
  - `src/main/db/schema/repairs.ts` — `backfillCompletedOnFinalPage`.
  - `src/main/webServer/routes/progress.ts`.
  - `src/main/ipc/readingHandlers.ts` — `reading:update-progress`, `reading:update-location`.
- **Per-user isolated progress, favorites, bookmarks, history**.
  - `src/main/db/progress.ts`, `src/main/db/favorites.ts`, `src/main/db/bookmarks.ts`, `src/main/db/history.ts`.
  - `src/main/webServer/routes/progress.ts` — bookmarks + favorites endpoints.
  - `src/main/webServer/routes/comics.ts` — favorites toggle on `/api/comics/:id/favorite`.

### Comic Reader (CBZ / CBR)

- **Page-at-a-time rendering with bounded prefetch**.
  - `src/main/archiveLoader.ts` — `getPage`.
  - `src/renderer/components/App.tsx` — LRU page cache + prefetch (`PAGE_CACHE_MAX`, `PREFETCH_AHEAD`).
  - `src/renderer/components/ReaderView.tsx` — comic reader view.
  - `src/web/views/reader/comicReader.js` — `loadPageImg` + neighbor preload.
- **Spread mode (1-page / 2-page)**, **zoom modes** (fit-height / fit-width / 1:1), **direction LTR/RTL**.
  - `src/web/views/reader/comicReader.js` — `applyZoom`, `applySpread`, `applyDirection`, persisted prefs.
  - `src/web/views/reader/prefs.js` — `loadReaderPrefs` / `saveReaderPrefs`.
  - CSS rules under `src/web/style.css` keyed by `data-zoom`, `data-spread`, `data-direction`.
- **Pinch-zoom + pan, swipe, tap zones, double-tap-to-zoom**.
  - `src/web/views/reader/comicReader/gestures.js` — `wirePinchPanSwipe`.
  - `src/web/views/reader/comicReader.js` — pan state + transform application.
- **Slide / fade / no transitions**.
  - `src/web/views/reader/comicReader.js` — `slide-from-{left,right}` classes; pref `transition`.
- **Bookmark page, toggle favorite, fullscreen, orientation lock**.
  - `src/web/views/reader/comicReader.js` — `bmBtn`, `favBtn`, `fsBtn`, `orientBtn` wiring.
  - `src/web/api.js` — `getBookmarks`, `createBookmark`, `deleteBookmark`, `addFavorite`, `removeFavorite`.
- **Page slider, page count display, page-change toast**.
  - `src/web/views/reader/utils.js` — `buildToolbar` (slider).
  - `src/web/views/reader/comicReader.js` — `hint` overlay + slider sync.
- **Keyboard shortcuts** (arrows / Space / Backspace / Home / End / F / Z / B / S / `+` / `-` / `0`).
  - `src/web/views/reader/comicReader/keyboard.js` — `wireKeyboard`.
  - `src/renderer/components/App.tsx` — Electron-side keyboard bindings.
- **Resume from last page**, **wake-lock**, **JXL decoding**.
  - `src/web/views/reader/state.js` — `acquireWakeLock` / `releaseWakeLock`.
  - `src/main/imageDecoder.ts` — `@jsquash/jxl` (WASM) → Sharp PNG.
  - `vite.main.config.ts` — externals list (keeps `@jsquash/jxl` and `sharp` out of the renderer bundle).

### EPUB Reader

- **Paginated rendering via `epubjs`**, 1-page / 2-page spread.
  - `src/renderer/components/EpubReaderView.tsx` (Electron).
  - `src/web/views/reader/epubReader.js` (web UI, loads epub.js + jszip from CDN).
- **Font family / size / theme picker** with localStorage persistence.
  - `src/web/views/reader/prefs.js` — `epubPrefs` + `saveEpubPrefs`.
  - `src/renderer/components/EpubReaderView.tsx` — same control set; reuses `FONT_FAMILIES` / `FONT_SIZES`.
- **Inline `!important` color/font enforcement** (beats author CSS).
  - `EpubReaderView.tsx` and `web/views/reader/epubReader.js` — both define `forceThemeOnContent` walking the iframe DOM.
- **Resume by CFI; chapter label from NCX/nav; progress %**.
  - Both EPUB readers; chapter label via `findChapterLabel`.
  - `src/main/ipc/readingHandlers.ts` + `src/main/webServer/routes/progress.ts` — progress write-through.
- **iframe-aware navigation** (arrow keys, swipe, click thirds, tap zones).
  - `src/web/views/reader/epubReader.js` — overlay div with `pointer-events:auto` zones (iPadOS-friendly).
  - `EpubReaderView.tsx` — keydown registered on each rendered iframe contents.

### PDF Reader

- **Canvas rendering via `pdfjs-dist` (legacy build, external worker)**.
  - `src/renderer/components/PdfReaderView.tsx`.
  - `src/web/views/reader/pdfReader.js`.
- **Fit-to-container scaling, DPR-aware sharpness**.
  - `PdfReaderView.tsx` — `scaleToFit` + `devicePixelRatio` math.
  - `src/shared/scaleFit.ts` + `src/shared/scaleFit.test.ts`.
- **Tap zones, swipe, keyboard nav; resume; cancellable render task on unmount**.
  - `PdfReaderView.tsx` — keydown handler + `renderTaskRef.cancel()` on cleanup.
  - `pdfReader.js` — same wiring for web UI.

### Web Server / Multi-user

- **Embedded HTTP server** (Node `http`-stdlib, default :8008, configurable; can run headless).
  - `src/main/webServer.ts` — `startWebServer`, dispatcher, headless plumbing.
  - `src/main/index.ts` — `--headless` / `CB8_HEADLESS=1` switch.
  - `src/main/ipc/webServerHandlers.ts` — `webserver:get-settings` / `webserver:set-settings`.
- **Same-origin web UI** served from `src/web/`.
  - `src/main/webServer/routes/staticFiles.ts` — `serveStatic` with traversal guard.
- **Auth via better-auth** (sign-up, sign-in by username or email, email verification, password reset).
  - `src/main/webServer/auth.ts` — `createAuth`, field mappings, plugins.
  - `src/main/webServer/routes/auth.ts` — `/api/auth/login`, `/logout`, `/register`, session probe.
  - `src/main/webServer/emailSender.ts` — pluggable email transport (console default).
  - `src/web/admin/login.js`, `signup.js`, `forgotPassword.js`, `resetPassword.js`, `session.js`.
- **Sessions in SQLite** (no in-memory map).
  - `src/main/db/schema/create.ts` — `session` table.
  - `src/main/db/schema/repairs.ts` — `backfillAccountFromPasswordHash` (legacy → better-auth).
  - `src/main/webServer/middleware.ts` — `ensureInitialAdmin` + helpers.
- **Admin role / admin-only endpoints**.
  - `src/main/webServer/context.ts` — `requireAdmin`, `RequestContext`.
  - `src/main/webServer/routes/users.ts` — `/api/users` CRUD (admin).
  - `src/main/webServer/routes/upload.ts` — admin uploads / pick-path / list-dir / add-path.
- **Default admin seeding + Reset Admin Password** menu item.
  - `src/main/webServer/middleware.ts` — `ensureInitialAdmin`.
  - `src/main/adminReset.ts` — `resetDefaultAdmin`.
  - `src/main/menu.ts` — "Reset Admin Password…" menu wiring.
- **Guest read-only mode toggle**.
  - `src/main/webServer/middleware.ts` — `isGuestAccessEnabled`, `GUEST_ACCESS_KEY`.
  - `src/main/webServer/routes/auth.ts` — `PUT /api/settings/guest-access`.
- **"Add from server path" with native folder picker** — host-only.
  - `src/main/webServer/routes/upload.ts` — `pick-path` + `add-path`.
  - `src/main/webServer/middleware.ts` — `isHostConnection` (loopback check).
  - `src/web/admin/addPath.js` — admin UI.
- **Streaming NDJSON scan progress**.
  - `src/main/webServer/routes/upload.ts` — `add-path` writes `{type, processed, discovered, …}\n`.
  - `src/main/webServer/ingest.ts` — `ingestPathStreaming` (server-side scan with progress events).
  - `src/web/api.js` — `adminAddPath` reader/decoder.
- **Path autocomplete** (`/api/admin/list-dir`).
  - `src/main/webServer/routes/upload.ts`.
- **Browser file/folder upload** with progress events.
  - `src/main/webServer/routes/upload.ts` — `/api/admin/upload` raw-body POST.
  - `src/web/api.js` — `adminUploadFile` (XHR with `upload.onprogress`).
  - `src/web/admin/upload.js` — admin UI.
- **Trusted origins include LAN IPv4**.
  - `src/main/webServer/auth.ts` — `resolveTrustedOrigins` enumerates `os.networkInterfaces()`.
- **Rate limiting** on login + forget-password.
  - `src/main/webServer/rateLimit.ts` — sliding-window in-memory limiter.
  - `src/main/webServer.ts` — pre-dispatch check.

### Web UI

- **Mobile-first responsive layout**, top nav + sidebar (desktop), bottom tab bar + tab-panel sheets (mobile).
  - `src/web/index.html` — markup skeleton.
  - `src/web/style.css` — layout + breakpoints.
  - `src/web/app.js` — bootstrapping + state setters.
  - `src/web/app/{router,sidebar,sideContextMenu,sort,state,tabPanel,toast}.js`.
- **Pull-to-refresh** on mobile.
  - `src/web/views/library.js` — `installPullToRefresh`.
- **Filter strips** (media, file type, read status, favorites pill).
  - `src/web/views/library/strips.js`.
- **Sort dropdown / sheet**.
  - `src/web/app/sort.js`.
  - `src/web/index.html` — `#sort-sheet` markup.
- **Routes** (`#/`, `#/continue`, `#/recent`, `#/library/{id}`, `#/folder/{id}`, `#/tag/{name}`, `#/read/{id}`, `#/reset-password`, `#/verified`).
  - `src/web/app/router.js`.
- **Card chrome** (cover, format badge, favorite heart, progress bar / %).
  - `src/web/views/library/cards.js` — `createCard`, `createFolderCard`, `formatBadgeFor`, `progressLabelFor`.
- **Right-click / long-press context menu** (rename, delete, tags, add to library/folder, refresh metadata).
  - `src/web/admin/contextMenu.js` — card context menu + tag editor.
  - `src/web/app/sideContextMenu.js` — sidebar collection/folder context menu.
- **Bulk-select with floating bar**.
  - `src/web/views/library/selection.js`.
- **Toast notifications**.
  - `src/web/app/toast.js`.
- **Drag-and-drop upload (admin)** (files + folders, recursive).
  - `src/web/admin/drop.js` — DataTransfer traversal.
  - `src/web/admin/upload.js` — orchestration + progress UI.
- **PWA manifest, theme-color, favicon**.
  - `src/web/manifest.json`, `src/web/favicon.svg`, `src/web/index.html` `<meta>`.
- **Cache-busting thumbnail URLs** (`?v={dateAdded ms}`).
  - `src/main/webServer/mapping.ts` — `toWebRecord` builds the URL.
- **Empty states** (offline / signed-out / no-results / no-recent / no-continue).
  - `src/web/views/library/empty.js` — `renderEmpty`, `emptyStateMarkup`.

### Electron Shell

- **Native window with custom application menu**.
  - `src/main/index.ts` — window lifecycle, `BrowserWindow` config.
  - `src/main/menu.ts` — `buildApplicationMenu`, recent files submenu.
- **"Open" file dialog** filtering to supported extensions.
  - `src/main/menu.ts` — File → Open.
- **"Open Recent" submenu** populated from the DB.
  - `src/main/menu.ts` — `buildRecentMenuItems`, drops missing files.
- **File associations + `open-file` event handling**.
  - `src/main/index.ts` — `app.on('open-file', …)`.
- **Settings dialog**: web-server enable / port, copy URLs.
  - `src/renderer/components/SettingsDialog.tsx` — UI.
  - `src/renderer/components/SettingsDialog.styles.ts` — co-located styles.
  - `src/main/ipc/webServerHandlers.ts` — settings persistence.
- **Headless mode**.
  - `src/main/index.ts` — `startHeadless` path.
- **macOS dock icon + branded `.app` name**.
  - `src/main/index.ts` — `app.dock?.setIcon`, `app.setName`.
  - `scripts/brand-dev-electron.mjs` — dev branding.
- **Squirrel install hooks** (Windows packaging).
  - `src/main/index.ts` — `electron-squirrel-startup` import.
- **Graceful `before-quit` teardown** (awaits archive close + DB checkpoint).
  - `src/main/index.ts` — `app.on('before-quit', …)`.

### Metadata

- **Editable per-comic fields** (title, author, artist, genre, year, summary, series, volume, chapter, external_id, external_source).
  - `src/main/db/comics.ts` — `updateComicMetadata`, `getComicMetadata`.
  - `src/main/webServer/routes/comics.ts` — `PUT /api/comics/:id/metadata`.
  - `src/renderer/components/library/DetailsModal.tsx` — Electron edit modal.
- **Remote metadata search** across ComicVine, AniList, MangaDex.
  - `src/main/metadataScraper.ts` — `searchMetadata`, per-source adapters.
  - `src/main/webServer/routes/comics.ts` — `GET /api/comics/:id/metadata-search`.
- **Apply candidate metadata + import cover URL** with SSRF guard.
  - `src/main/webServer/routes/comics.ts` — applies metadata, then `safeFetchBuffer(coverUrl)`.
  - `src/main/webServer/safeFetch.ts` — `safeFetchBuffer` (https-only, private-IP block, 10 s timeout, 16 MiB cap).

### Resource Management & Performance

- **Refcounted archive handle cache**.
  - `src/main/webServer/archiveCache.ts` — `withArchive`, `evictFromCache`, `closeAllHandles`.
- **CBR per-handle 64 MiB LRU page cache**.
  - `src/main/archiveLoader.ts` — `_pageCache` / `_pageCacheBytes` on `CbrHandle`, `evictCbrCache`.
- **On-disk image-resize cache** with 2 GiB cap.
  - `src/main/imageResizer.ts` — `getCachedOrResize`, `evictIfOverBudget`.
  - `src/main/webServer/routes/comics.ts` — page + thumbnail width-resize hooks.
- **Page bytes shipped over IPC as raw `ArrayBuffer`**.
  - `src/main/ipc/archiveHandlers.ts` — `archive:page` returns `{ buffer, mime }`.
  - `src/renderer/components/App.tsx`, `ReaderView.tsx` — `Blob` URL wrapping + revocation.
- **Sharp resize with `withoutEnlargement` + WebP re-encode**.
  - `src/main/imageResizer.ts` — `resizeImage`, `.webp({ quality: 82 })`.
- **Body-size cap** on the embedded server.
  - `src/main/webServer/middleware.ts` — `readBody(req, maxBytes)`, `BodyTooLargeError`.
  - `src/main/webServer/routes/upload.ts` — admin endpoints opt into the larger cap explicitly.

### Security

- **bcrypt password hashing + better-auth account migration**.
  - `src/main/webServer/auth.ts` — `password.hash` / `password.verify` config.
  - `src/main/db/schema/repairs.ts` — `backfillAccountFromPasswordHash`.
- **Tightened renderer CSP**.
  - `src/renderer/index.html` — `<meta http-equiv="Content-Security-Policy" …>` with sha256-pinned inline `<style>` and `script-src 'self'`.
- **Path-traversal guards** on uploads + static serving.
  - `src/main/webServer/routes/upload.ts` — relative-path validation, `path.resolve` containment check.
  - `src/main/webServer/routes/staticFiles.ts` — `path.normalize` + `startsWith(staticRoot)`.
- **Rate-limited auth surfaces**.
  - `src/main/webServer/rateLimit.ts`, `src/main/webServer.ts`.
- **SSRF guard on remote cover fetches**.
  - `src/main/webServer/safeFetch.ts`.
- **Filename + relative-path validation on upload**.
  - `src/main/webServer/routes/upload.ts` — `isBad(...)`, basename check, extension allowlist.

### Quality / DevX

- **React error boundaries** around top-level views.
  - `src/renderer/components/ErrorBoundary.tsx`.
  - `src/renderer/components/App.tsx` — wraps `LibraryView`, `EpubReaderView`, `PdfReaderView`, `ReaderView`.
- **In-app `ConfirmModal`** replacing every `window.confirm` / `window.alert`.
  - `src/renderer/components/ConfirmModal.tsx`.
  - `src/renderer/components/useConfirm.tsx` — hook + provider.
- **Real error message + stack** returned on 500s (local-network deployment).
  - `src/main/webServer.ts` — top-level catch.
- **118 unit tests** (Vitest) over shared utilities.
  - `src/shared/{coverSelection,dropValidator,imageFilter,naturalSort,scaleFit,statusFormat,windowTitle}.test.ts`.
  - `src/main/seriesParser.test.ts`.
- **Search benchmark script**.
  - `scripts/bench-search.mjs`.
- **Strict TypeScript** across renderer / main / shared.
  - `tsconfig*.json`.
- **Forge packagers** for DMG, deb, RPM.
  - `forge.config.*`, `vite.main.config.ts`, `vite.preload.config.ts`, `vite.renderer.config.ts`.

---

## Planned / backlog

Ordered top-down by user impact and likelihood of being requested.

### Tier 1 — Foundational gaps

1. **Local file watching** — FSEvents/inotify on scan folders so newly-dropped files appear without manual rescan.
2. **Continuous-scroll reading mode** for manga / webtoons (vertical strip).
3. **Duplicate detection** — flag the same content added twice under different paths.
4. **Per-user reader preferences synced server-side** — font / size / direction / theme follow the user across devices instead of living in `localStorage`.
5. **Offline PWA** — service-worker cache so recently-read items keep working without the server.
6. **Cover regeneration** — re-extract thumbnails for a selection (covers existing items being rebuilt after a metadata change).
7. **Brightness / contrast / sepia overlay** in the comic reader (eye comfort, OLED).

### Tier 2 — High value, well-scoped

8. **Bulk metadata edit** — set series / tags / rating across a multi-select.
9. **Star ratings** (1–5) — column, filter, sort.
10. **Persistent zoom level per comic.**
11. **Page cropping / auto-trim** to strip white/black borders.
12. **Scan scheduling** — cron-style "rescan nightly" in the embedded server.
13. **Storage analytics** — breakdown by format / series / library, identify space hogs.
14. **Broken file detection** — flag CBZs that fail to extract during scan.
15. **Reading streak / stats** — pages-per-day chart, session history (the `reading_history` table is already populated).
16. **EPUB highlight + annotation** — saved passages with notes; export to Markdown.
17. **EPUB table of contents panel** as a sidebar.
18. **Smart collections** — saved filter presets ("unread · CBZ · added this month").
19. **Reading lists / queues** — ordered lists separate from collections.
20. **Multi-user invite links** — token-based instead of admin-creates-user.

### Tier 3 — Nice to have

21. **Random pick button** ("open a random unread from this view").
22. **"New additions" shelf** distinct from the date-sorted grid.
23. **Related-by-series sidebar** when a comic is open.
24. **Reading time estimate** — "~12 min left at your pace".
25. **EPUB custom fonts**, **line-height / margin** controls.
26. **EPUB "Chapter 4 of 11"** progress display alongside %.
27. **Double-page cover detection** — page 0 always single in spread mode.
28. **Fit-to-content zoom** — auto-pick fit-width vs fit-height per page.
29. **Auto-advance / slideshow** timer for fixed-layout comics.
30. **Hardware media keys / presentation clicker** support in the Electron reader.
31. **Touch Bar support** (Mac) for page slider and bookmark.
32. **Export reading log** (CSV).
33. **Printing** — current page or range.
34. **Audit log** — who opened what, when (opt-in).
35. **Shared reading / book club mode** — sync position between two viewers of the same book.
36. **Per-user "download for offline"** — direct CBZ/EPUB download from the web UI.
37. **System tray** with background scan notifications.
38. **Keyboard shortcut reference** (`?` overlay).
39. **Mobile add-to-home-screen prompt.**

### Tier 4 — Imports / interop

40. **Import from Calibre** — read `metadata.opf`.
41. **Import from Komga** — read `series.json`.
42. **Guided-view panel detection** (manga panels). Complex; experimental.
43. **Dictionary lookup** in EPUB reader.

### Accessibility (cross-cutting)

- High-contrast UI theme.
- Screen-reader labels on every interactive control.
- Configurable swipe sensitivity (motor accessibility).
- Large-text UI mode independent of OS scaling.

These are scoped together because they're best done as a single accessibility pass rather than scattered through the tiers above.
